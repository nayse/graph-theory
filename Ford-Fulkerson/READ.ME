Esse algoritmo nao aceita o comando -s, e os valores de -i sera o primeiro vertice no caso 1, e o -l sera o ultimo vertice no caso 6;
o arquivo ser executado atraves do comando 'make' no terminal, logo em seguida o exemplo de execuçao no txt Execuçao;
